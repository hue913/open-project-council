# Open Project Council 术语表

## 用户

通过 GitHub 身份验证并拥有一个工作区身份的个人。用户只会看到自己是成员的项目。

## 项目

由一个所有者创建的私有工作空间，承载任务、运行记录、代理席位、交付连接和审计记录。项目内容始终归项目所有者与其授权成员所有。

## 项目成员

被授予访问某个项目的用户。成员角色是 `owner`、`editor` 或 `viewer`；角色决定可读取、编辑和交付的范围。

## 凭据归属

一个加密凭据只属于单个用户或单个项目连接。凭据明文只在获授权的单次运行中短时使用，绝不返回浏览器或写入审计正文。

## 代理席位

项目内一个具名的模型、桌面 Agent 或 MCP 工具配置。席位不等于成员；它只能在项目授权的任务中运行。

## 运行

一次任务的可审计执行记录，包含独立提案、质疑、裁决、执行与验证。分歧保留为未解决风险。

## 桌面桥接

由本地执行器主动建立的出站连接。它可以领取自己获准的任务，不能接受互联网对用户电脑发起的入站连接。

## 交付确认

项目所有者对外部副作用的可追踪同意。GitHub 分支与 PR、Vercel 预览需要已授权连接；生产部署还必须有当次显式确认。
